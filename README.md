# CMSC 126 Lab 3

Simple HTML website project.

## Setup (Recommended)

1. Clone the repository
2. Install Live Server globally using `npm install -g live-server`
3. Open a terminal in the project folder and run `live-server` to start the server
4. Your default browser will open the site and automatically refresh on changes
